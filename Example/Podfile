use_frameworks!

platform :ios, '10.0'

target 'Gleap_Example' do
  pod 'Gleap', :path => '../'
  pod 'Alamofire', '~> 5.2'
  pod 'AFNetworking', '~> 4.0'
  
  target 'Gleap_Tests' do
    inherit! :search_paths
    
  end
end
